

# 1. Run lint-staged for formatting (gofmt, prettier)
echo "🎨 Running Formatter..."
npx lint-staged

# 2. Run Backend Vet (Go) - checks for correctness
echo "🐘 Running Backend Vet..."
(cd backend && go vet ./...)

# 3. (Removed) Full lint check is too slow and blocks legitimate changes.
# Relying on lint-staged (Step 1) to check modified files.

